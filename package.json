{
  "name": "console.unicorn",
  "version": "0.1.0",
  "scripts": {
    "test": "jest",
    "release": "np --no-2fa --no-draft-release"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^25.3.0",
    "prettier": "^2.0.4"
  }
}
